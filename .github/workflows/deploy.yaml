name: Deploy to RCade

on:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: "0 6 * * *"

jobs:
  build-and-deploy:
    name: Build and Deploy to RCade
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: bun install
      - name: Fetch feed
        run: bun run fetch
        env:
          BLAGGREGATOR_TOKEN: ${{ secrets.BLAGGREGATOR_TOKEN }}
      - name: Build Vite project
        run: bun run build
      - name: Deploy to RCade
        uses: fcjr/rcade/action-deploy@main
